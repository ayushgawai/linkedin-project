/** Hacker News + Dev.to lists — how often to refetch while the page is in the foreground. */
export const EXTERNAL_NEWS_REFETCH_INTERVAL_MS = 5 * 60 * 1000

/** Remotive (external job strip) — slower cadence, larger payload. */
export const EXTERNAL_JOBS_REFETCH_INTERVAL_MS = 10 * 60 * 1000

/** Right-rail promoted card: switch creative while the slot is visible. */
export const RAIL_AD_ROTATE_INTERVAL_MS = 2 * 60 * 1000
