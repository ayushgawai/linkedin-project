# Optional Compose overrides (copy to `infra/.env` — Docker Compose loads it automatically).
#
# API_GATEWAY_POSTS_URL=http://posts:8008
#
# OpenAI — same key for resume/outreach/embeddings and Career Coach narrative:
# OPENAI_API_KEY=sk-...
# COACH_LLM_ENABLED=true
# COACH_LLM_MODEL=gpt-4o-mini
# COACH_LLM_TIMEOUT_SEC=45
