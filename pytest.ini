[pytest]
testpaths = tests
addopts =
    --cov=data
    --cov-report=term-missing
    --cov-report=html:tests/coverage
    -v

markers =
    integration: end-to-end integration tests spanning multiple services
    failure: failure mode tests (error codes and edge cases)
